#!/usr/bin/env sh
set -eu

if [ ! -x .venv/bin/python ]; then
  echo "Set up the test environment first: python3 -m venv .venv && .venv/bin/python -m pip install -r requirements_test.txt" >&2
  exit 1
fi

exec .venv/bin/python -m pytest "$@"
